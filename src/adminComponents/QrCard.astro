---
const {user} = Astro.props

---


<div class="bg-white p-6 rounded-lg shadow-lg" >
    <h1 class="text-2xl font-bold mb-4">{user.username}</h1>
    <p class="text-gray-700 mb-6">
      ¡Bienvenido! Aquí puedes escanear el código QR para conectar tu
      WhatsApp al bot.
    </p>
    {user.qrCode ? (
      <img src={user.qrCode} alt="QR Code" class="mx-auto w-1/2 h-1/2" />
    ) : (
      <p class="text-red-500">No QR code available</p>
    )}
  </div>